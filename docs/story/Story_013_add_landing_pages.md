I want to make /pdf-tools, /image-tools, /developer-tools, /video-tools landing pages that list the tools in each category. This will be a new story, Story_013_add_landing_pages.md. The landing pages should be accessible from the navbar and should have a consistent design with the rest of the site. Each tool listed on the landing page should link to its respective page.

# /pdf-tools be linked from "PDF" on the navbar.
- This landing page should have a header "PDF Tools" and a brief description of the tools available in this category.
- It should have a header section that explains what PDF tools are and how they can be useful for users.
- Each tool should be displayed as a card with the tool name, a screenshot, a long description, and a link to the tool's page.
- The design should be consistent with the rest of the site, using the same color scheme and typography.

# /image-tools be linked from "Image" on the navbar.
- This landing page should have a header "Image Tools" and a brief description of the tools available in this category.
- It should have a header section that explains what image tools are and how they can be useful for users.
- Each tool should be displayed as a card with the tool name, a screenshot, a long description, and a link to the tool's page.
- The design should be consistent with the rest of the site, using the same color scheme and typography.

# /developer-tools be linked from "Developer" on the navbar.
- This landing page should have a header "Developer Tools" and a brief description of the tools available in this category.
- It should have a header section that explains what developer tools are and how they can be useful for users.
- Each tool should be displayed as a card with the tool name, a screenshot, a long description, and a link to the tool's page.
- The design should be consistent with the rest of the site, using the same color scheme and typography.

# /video-tools be linked from "Video" on the navbar.
- This landing page should have a header "Video Tools" and a brief description of the tools available in this category.
- It should have a header section that explains what video tools are and how they can be useful for users.
- Each tool should be displayed as a card with the tool name, a screenshot, a long description, and a link to the tool's page.
- The design should be consistent with the rest of the site, using the same color scheme and typography.

